<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>

  <!-- Optional local override for Trax.* package versions during cross-repo dev.
       Generated by pack-local.sh; gitignored. Sets $(TraxLocalVersion) when present. -->
  <Import Project="trax-local.props" Condition="Exists('trax-local.props')" />

  <!-- Trax cross-repo packages. CI uses the exact pin; trax-local.props overrides to the locally-packed
       1.99.99 during cross-repo dev. -->
  <ItemGroup>
    <PackageVersion
      Include="Trax.Effect.StateMachine.Persistence"
      Version="$([MSBuild]::ValueOrDefault('$(TraxLocalVersion)', '1.50.0'))"
    />
  </ItemGroup>

  <!-- Third-party packages (exact pins). -->
  <ItemGroup>
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="DotNet.ReproducibleBuilds" Version="2.0.5" />
    <PackageVersion Include="FluentAssertions" Version="8.10.0" />
    <PackageVersion Include="GraphQL-Parser" Version="9.5.1" />
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp" Version="5.9.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageVersion Include="Microsoft.OpenApi.Readers" Version="1.6.31" />
    <PackageVersion Include="NUnit" Version="4.6.1" />
    <PackageVersion Include="NUnit.Analyzers" Version="4.14.0" />
    <PackageVersion Include="NUnit3TestAdapter" Version="6.3.0" />
    <PackageVersion Include="PublicApiGenerator" Version="11.5.4" />
    <PackageVersion Include="Scriban" Version="7.2.6" />
    <PackageVersion Include="System.CommandLine" Version="2.0.11" />
  </ItemGroup>
</Project>
